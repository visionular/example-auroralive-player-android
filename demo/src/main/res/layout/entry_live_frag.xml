<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".demo.EntryLiveFragment"
    android:padding="5dp">

    <EditText
        android:id="@+id/playback_id_edit_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="playback id"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:inputType="textNoSuggestions"/>

    <EditText
        android:id="@+id/token_edit_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="token"
        app:layout_constraintTop_toBottomOf="@id/playback_id_edit_view"
        android:inputType="textNoSuggestions"/>

    <Button
        android:id="@+id/entry_live_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="play"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/token_edit_view" />
</androidx.constraintlayout.widget.ConstraintLayout>